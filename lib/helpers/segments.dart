import 'package:flutter/material.dart';

class Segment {
  const Segment({
    required this.color,
    required this.start,
    required this.size,
  });

  final Color color;
  final double start;
  final double size;
}
